#tuples is immutable(cannot modify) and indexed
#application in places where data can't be modified

#tuples inside a list
coordinates = [(4, 5), (6, 8), (5, 9)]
print(coordinates[0])

#coordinates[1] = 10
print(coordinates[1])